spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://auth-service:8080/.well-known/jwks.json

spring.security.oauth2.resourceserver.jwt.issuer-uri=http://auth-service:8080

# MinIO configuration
minio.endpoint=http://minio:9000
minio.access-key=${MINIO_ACCESS_KEY}
minio.secret-key=${MINIO_SECRET_KEY}
minio.bucket=erp-assets
minio.public-endpoint=http://localhost:9000

# Order module internal item lookup
order.item-service.base-url=http://localhost:${SERVER_PORT:8082}
