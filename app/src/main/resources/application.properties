server.port=8080

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/erp}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

myerp.minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
myerp.minio.access-key=${MINIO_ACCESS_KEY:minioadmin}
myerp.minio.secret-key=${MINIO_SECRET_KEY:minioadmin}
myerp.minio.bucket=${MINIO_BUCKET:myerp}


#jwt
security.jwt.secret=${JWT_SECRET:NO_ONE_CAN_FUCK_ME}
security.jwt.issuer=${JWT_ISSUER:myerp}
security.jwt.access-token-minutes=${JWT_ACCESS_MINUTES:500}
